version: "3.8"
services:
  backend:
    build: .
    image: wandergenie-backend:latest
    container_name: wandergenie-backend
    ports:
      - "8000:8000"
    # For local/dev only: supply env at runtime. Do NOT commit real secrets.
    env_file:
      - backend/.env
    restart: unless-stopped

